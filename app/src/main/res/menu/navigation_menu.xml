<?xml version="1.0" encoding="utf-8"?>
<menu
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
        android:id="@+id/matches_menu"
        android:icon="@drawable/ic_matches"
        android:title="@string/matches_title"
        android:enabled="true"
        app:showAsAction="ifRoom"/>
    <item
        android:id="@+id/teams_menu"
        android:icon="@drawable/ic_teams"
        android:title="@string/teams_title"
        android:enabled="true"
        app:showAsAction="ifRoom" />
    <item
        android:id="@+id/favorites_menu"
        android:icon="@drawable/ic_favorites"
        android:title="@string/favorites_title"
        android:enabled="true"
        app:showAsAction="ifRoom" />
</menu>